---
- hosts: all
  remote_user: mayur.shivakumar
  sudo: yes
  vars:
    home_user: mayur.shivakumar
    workspace: /home/{{ home_user }}/Documents
    git_name: Camron Levanger
    git_email: mayur.shivakumar@insidesales.com

  tasks:
    - name: Update APT package cache
      action: apt update_cache=yes

    - include: utils.yml

    - include: lamp.yml

    - include: js-devel.yml

    - include: desktop.yml

    - include: conf.yml
